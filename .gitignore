# Data — never commit datasets / embeddings / model artifacts
# (anchored to repo root so it does NOT match the src/mtg_draft_ml/data/ package)
/data/
*.csv
*.csv.gz
*.parquet
*.npy
*.npz
*.pt
*.ckpt
*.onnx
embeddings_cache/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/
.ruff_cache/

# Env / secrets
.env
*.local

# OS / editor
.DS_Store
.idea/
.vscode/
